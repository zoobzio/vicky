root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/vicky ./cmd/vicky"
  bin = "./tmp/vicky"
  include_ext = ["go", "tpl", "tmpl", "html"]
  exclude_dir = ["tmp", "vendor", "migrations"]
  delay = 1000

[log]
  time = false
